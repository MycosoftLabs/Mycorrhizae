# =============================================================================
# Mycorrhizae Protocol - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in real values.
# NEVER commit .env to git. Only .env.example should be in version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Database + Redis (MINDEX VM is canonical)
# -----------------------------------------------------------------------------
MYCORRHIZAE_DATABASE_URL=postgresql://user:password@192.168.0.189:5432/mindex
MYCORRHIZAE_REDIS_URL=redis://192.168.0.189:6379

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
MYCORRHIZAE_HOST=0.0.0.0
MYCORRHIZAE_PORT=8002

# -----------------------------------------------------------------------------
# CORS
# -----------------------------------------------------------------------------
MYCORRHIZAE_CORS_ORIGINS=*

# -----------------------------------------------------------------------------
# API Key Bootstrap (one-time)
# -----------------------------------------------------------------------------
# Used ONLY to mint the very first admin key via POST /api/keys/bootstrap.
# After you have an admin key, you can rotate/revoke/create keys via /api/keys/*.
MYCORRHIZAE_BOOTSTRAP_TOKEN=your-random-bootstrap-token

# -----------------------------------------------------------------------------
# Envelope verification policy
# -----------------------------------------------------------------------------
# 1 = enforce Ed25519 signature validation on incoming envelopes
# 0 = accept envelopes but mark signature invalid when missing/bad
MYCORRHIZAE_REQUIRE_DEVICE_SIGNATURE=0

